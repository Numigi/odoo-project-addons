<?xml version="1.0" encoding="UTF-8"?>
<odoo>

    <record id="project_type_form_with_default_task_stages" model="ir.ui.view">
        <field name="name">Project Type Form: add default task stages</field>
        <field name="model">project.type</field>
        <field name="inherit_id" ref="project_type.project_type_form"/>
        <field name="arch" type="xml">
            <field name="sequence" position="after">
                <field name="default_task_stage_ids" widget="many2many_tags"/>
            </field>
        </field>
    </record>

</odoo>
