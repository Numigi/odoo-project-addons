<?xml version="1.0" encoding="UTF-8"?>

<odoo>

    <!-- Form View -->
    <record id="project_milestone_start_date_view_form" model="ir.ui.view">
        <field name="name">Project Milestone Form: Add Start Date</field>
        <field name="model">project.milestone</field>
        <field name="inherit_id" ref="project_milestone.project_milestone_view_form"/>
        <field name="arch" type="xml">
            <field name="target_date" position="before">
                <field name="start_date"/>
            </field>
        </field>
    </record>

    <!-- List View -->
    <record id="project_milestone_start_date_view_list" model="ir.ui.view">
        <field name="name">Project Milestone List: Add Start Date</field>
        <field name="model">project.milestone</field>
        <field name="inherit_id" ref="project_milestone.project_milestone_view_list"/>
        <field name="arch" type="xml">
            <field name="target_date" position="before">
                <field name="start_date"/>
            </field>
        </field>
    </record>

</odoo>
