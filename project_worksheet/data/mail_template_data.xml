<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="0">
        <record id="email_template_worksheet_approval" model="mail.template">
            <field name="name">Worksheet: Approval Request</field>
            <field name="model_id" ref="project_worksheet.model_project_worksheet"/>
            <field name="subject">Worksheet ${object.name} - project ${object.project_id.name} - approval required</field>
            <field name="email_to">${object.partner_approver_id.email}</field>
            <field name="body_html" type="html">
<div style="margin: 0px; padding: 0px; font-size: 13px; color: #444444;">
    <p style="margin: 0px 0px 16px 0px;">
        Hello ${object.partner_approver_id.name or 'Client'},
    </p>
    <p style="margin: 0px 0px 16px 0px;">
        The installation hours performed on your site on ${object.date_start} are ready for approval. The details can be found in the attached worksheet.
    </p>

    <table border="0" cellpadding="4" cellspacing="0" style="margin-top: 16px; margin-bottom: 24px; width: 100%; max-width: 400px;">
        <tr>
            <td style="color: #888888; width: 120px;">Worksheet</td>
            <td><strong>${object.name}</strong></td>
        </tr>
        <tr>
            <td style="color: #888888;">Project</td>
            <td>${object.project_id.name}</td>
        </tr>
        <tr>
            <td style="color: #888888;">Supervisor</td>
            <td>${object.supervisor_id.name}</td>
        </tr>
        <tr>
            <td style="color: #888888;">Total Hours</td>
            <td><strong>${object.total_hours}</strong></td>
        </tr>
    </table>

    <div style="margin: 24px 0px 24px 0px;">
        <a href="${object.get_base_url()}/my/worksheet/${object.id}/${object.access_token}"
           target="_blank"
           style="background-color: #F89406; padding: 10px 20px; text-decoration: none; color: #ffffff; border-radius: 3px; font-size: 14px; font-weight: bold;">
            Review and approve the worksheet
        </a>
    </div>

    <p style="color: #888888; font-size: 11px; margin-top: 32px;">
        This link is personal and remains valid for 14 days. The approval only covers the installation hours performed on site.
    </p>
</div>
            </field>
            <field name="lang">${object.partner_approver_id.lang}</field>
            <field name="auto_delete" eval="True"/>
        </record>
    </data>
</odoo>