<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <!-- Form View -->
    <record id="project_active_view_form" model="ir.ui.view">
        <field name="name">Project Active Form</field>
        <field name="model">project.project</field>
        <field name="inherit_id" ref="project.edit_project"/>
        <field name="arch" type="xml">
            <xpath expr="//div[hasclass('oe_button_box')]" position="inside">
                <button name="toggle_active" string="Archive" type="object" attrs="{'invisible': [('active', '=', False)]}"
                        confirm="Archiving a project automatically archives its tasks and milestones. Do you want to proceed ?"
                        icon="fa-archive"/>
                <button name="toggle_active"
                        type="object"
                        confirm="Unarchiving a project automatically unarchives its tasks and milestones. Do you want to proceed ?"
                        class="oe_stat_button"
                        string="Unarchive"
                        icon="fa-archive"
                        attrs="{'invisible': [('active', '=', True)]}" >
                </button>
            </xpath>
            <xpath expr="//page[@name='settings']" position="before">
                <field name="show_milestones" invisible="1"/>
                <page name="milestones" string="Milestones" attrs="{'invisible': [('show_milestones', '=', False)]}">
                    <field name="milestone_ids" context="{'default_project_id': active_id}" >
                        <tree string="Milestones">
                            <field name="name"/>
                            <field name="deadline"/>
                            <field name="is_reached"/>
                            <field name="task_ids" widget="many2many_tags"/>
                        </tree>
                    </field>
                </page>
            </xpath>
        </field>
    </record>
</odoo>
