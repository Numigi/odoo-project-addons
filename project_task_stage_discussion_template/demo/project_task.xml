<?xml version="1.0"?>
<odoo>
    <data noupdate="1">

        <record id="msg_discus1" model="mail.message">
            <field name="subject">Ticket Open</field>
            <field name="body">
                <![CDATA[<p>Hello,</p><p>this is an automated message to inform you that a ticket was open in your project.</p>]]></field>
            <field name="message_type">email</field>
            <field name="subtype_id" ref="mail.mt_comment"/>
            <field name="author_id" ref="base.partner_root"/>
            <field name="model">project.task</field>
            <field name="partner_ids" eval="[(6, 0, [ref('base.res_partner_2')])]"/>
            <field name="date" eval="(DateTime.today() - timedelta(days=2)).strftime('%Y-%m-%d %H:%M')"/>
        </record>
    </data>
</odoo>